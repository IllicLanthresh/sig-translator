"""PySide6 (Qt) UI for sig-translator — the redesigned single-window app + overlays.

The capture/OCR/translate/materials/mining/config logic lives in the parent package
and is reused unchanged; this package only owns presentation.
"""
